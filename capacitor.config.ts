import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vibe.market',
  appName: 'vibe-protocol',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;

